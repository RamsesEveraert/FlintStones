#pragma once
#include <string>
#include <vector>
class FileManager final // NOT USED YET
{
public:

	FileManager();
	~FileManager();

	/*static std::vector<unsigned char> ReadFromFile(const std::string& fileName);*/
	/*static std::string ReadFromTextFile(const std::string& fileName);*/
private:

};

/*						

							CREDITS:
					
					- hulp Liam

*/			